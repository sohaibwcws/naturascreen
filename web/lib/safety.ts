// Concise, always-visible reminders for the persistent UI chrome. The AUTHORITATIVE,
// full-length disclaimer and simulation notice are owned by the backend
// (naturascreen/disclaimer.py) and are embedded into every results/report/export payload
// and rendered verbatim there. These short strings never substitute for those; they keep
// the boundary in view on every page so it is never out of sight.

export const BANNER_DISCLAIMER =
  "Research hypotheses only — not a treatment, cure, dose, or medical advice. Real validation requires lab and clinical work.";

export const SIM_BADGE = "Illustrative, not predicted";
